# API Gateway

Single entry point for the Auto Rentals platform. Handles routing, authentication, rate limiting, and request validation.

## Scripts
- `npm run dev` - start with nodemon
- `npm start` - start in production mode

## Environment Variables
Copy `.env.example` to `.env` and adjust as needed.
